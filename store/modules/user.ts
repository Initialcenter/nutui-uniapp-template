import { defineStore } from 'pinia'
export const useUsersStore = defineStore('users', {
  state: () => ({
    // 定义你的 state
  }),
  actions: {
    // 定义你的 actions
  	
  },
  getters: {
    // 定义你的 getters
  }
})
